--[[
    Color3: https://developer.roblox.com/en-us/api-reference/datatype/Color3

    Color3 is just a getter for colors.
    Color3 has methods to get colors:
    from RGB [0-255], (red, green, blue)
    from percent [0-1],
    from HSV (or HSL) [0-1] (hue, saturation, and value/lightness)
    from HEX (or hexadecimal), looks like this -> #FF0000
]]

--wip
